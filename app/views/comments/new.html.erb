<h1><%= t('views.common.title_new', name: Comment.model_name.human.downcase) %></h1>

<%= render "comments/form", comment: @comment, book: @book %>

<br>

<div>
  <%= link_to t('views.common.back', name: i18n_pluralize(Comment.model_name.human.downcase)), comments_path %>
</div>
