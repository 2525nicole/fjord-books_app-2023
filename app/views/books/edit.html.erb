<h1><%= t('title.edit', model: Book.model_name.human) %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('views.show'), @book %> |
  <%= link_to t('views.back', model: Book.model_name.human), books_path %>
</div>
